# this file's purpose in life is to make the parrent directory able to be
# recognized by django as a package so the tests.py (sibling file) can be
# detected as a tester file when running:
"python3 manage.py test"
