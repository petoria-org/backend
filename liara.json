{
  "app": "petoria-back",
  "port": 80,
  "build": {
    "location": "iran"
  },
  "disks": [],
  "args": [
    "bash -lc 'python manage.py migrate --noinput && python manage.py collectstatic --noinput && daphne -b 0.0.0.0 -p ${PORT:-80} petoria.asgi:application'"
  ]
}
